%h1 Editing post

= render 'form'

%br
= link_to 'Show', game_event_post_path(@post.game, @post.event, @post), class: 'pure-button'
= link_to 'Back', game_event_posts_path(@post.game, @post.event), class: 'pure-button'
