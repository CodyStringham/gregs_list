.pure-g
  .pure-u-1.pad-10
    %p
      %b Title:
      = @post.title
    %p
      %b Checkpoint:
      = @post.checkpoint
    %p
      %b User:
      = @post.user
    %p
      %b Event:
      = @post.event.name

.pure-g
  .pure-u-1.pad-10
    = link_to 'Edit', edit_game_event_post_path(@post.game, @post.event, @post), class: 'pure-button'
    = link_to 'Back', game_event_posts_path(@post.game, @post.event), class: 'pure-button'
